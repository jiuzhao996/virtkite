# IT Tools 1.0.0 —— 版本级表单定义（纯静态站点，仅暴露一个端口）
additionalProperties:
  formFields:
    - envKey: PANEL_APP_PORT
      label: 服务端口
      type: number
      default: 8088
      required: true
      rule: paramPort
